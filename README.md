A UniFi dashboard that uses the API to display data.

Created with Claude and updated to be portable to any site. It stores data in an SQLite database and keeps per-minute history where it makes sense.

Store the gateway admin credentials in the .env file; env.example is provided as the template.
